import { createTheme } from '@mui/material';
export const customTheme = createTheme({
  palette: {
    primary: {
      main: '#2196f3',
    },
    secondary: {
      main: '#fc4d4d',
    },
  },
});
